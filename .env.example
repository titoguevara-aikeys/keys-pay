# AIKEYS Financial Platform - Environment Configuration
# Copy this to .env and configure with your values

# === SECURITY & MONITORING ===
# Force full monitoring (production default: true)
FORCE_FULL_MONITORING=true

# Feature flag storage backend
FLAG_STORE=supabase

# Admin API secret (TEMPORARY - replace with proper RBAC)
VITE_ADMIN_API_SECRET=change-me-to-secure-secret

# === SUPABASE CONFIGURATION ===
# Supabase project configuration
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# === EXTERNAL APIS ===
# OpenAI for AI features
OPENAI_API_KEY=your-openai-api-key

# Resend for email services
RESEND_API_KEY=your-resend-api-key

# Stripe for payments
STRIPE_SECRET_KEY=your-stripe-secret-key

# === BETA TESTING FLAGS ===
# Override specific flags via environment (bypasses database)
# FLAG_BETA_MONITORING=off